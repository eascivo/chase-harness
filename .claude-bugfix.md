# Task: Fix Bugs from Code Review

## Context
Code review found the following bugs in chase-harness. Fix ALL of them. Do NOT touch README files.

## Bugs to Fix

### 🔴 Critical

**1. `_fix_unescaped_quotes_in_json` can corrupt JSON — add logging**
File: `chase/subprocess.py`
- Add `import logging` at module level (if not already present), create `logger = logging.getLogger(__name__)`
- In `extract_json_from_text`, add `logger.debug("LLM JSON quirks fix attempted")` before the `_fix_llm_json_quirks` call
- In `_fix_unescaped_quotes_in_json`, add `logger.warning("Fixed unescaped quotes in JSON (heuristic)")` when an escape happens

**2. `_fix_llm_json_quirks` curly-quote replacement runs on full text before JSON extraction**
File: `chase/subprocess.py`
- Move the smart-quote replacement logic so it only applies INSIDE the extracted JSON candidate, not the full text
- Refactor: remove smart-quote handling from `_fix_llm_json_quirks`, instead apply it inside the regex match loop after a candidate is found but before `json.loads`

### 🟡 Medium

**3. `sync_config` auto-sets `approved = True` on completion/failure**
File: `chase/ray/sync.py`
- In `sync_project`, when a project reaches `completed` or `needs_review` state, do NOT set `approved = True` or stamp `approved_at`
- Only set approved status if the project was already approved before sync
- Exception: if `project.approved` was already True, keep it (don't un-approve)

**4. `cmd_status` in `ray/cli.py` modifies state as side effect**
File: `chase/ray/cli.py`
- Remove the `sync_config(config)` and `state.save_queue(config)` calls from `cmd_status`
- Status should be read-only

**5. `_project_contracts` reads `.md` files as raw JSON — too fragile**
File: `chase/ray/cli.py`
- Import `extract_json_from_text` from `chase.subprocess`
- Use it instead of `json.loads(content)` for parsing contract files
- Add a logger warning when direct JSON parse fails but `extract_json_from_text` succeeds

**6. `test_ray_trust.py` has duplicated test from `test_trust.py`**
File: `tests/test_ray_trust.py`
- Remove the duplicated `test_orchestrator_writes_verification_card` test
- Keep it only in `tests/test_trust.py`

### 🟢 Minor

**7. Unused import `bold` in `test_ray_inspect.py`**
File: `tests/test_ray_inspect.py`
- Remove `bold` from the import line, keep only `green`

**8. Inconsistent ANSI color usage in `_STATUS_COLORS`**
File: `chase/ray/cli.py`
- Replace raw ANSI escape codes in `_STATUS_COLORS` with the corresponding helpers from `chase.fmt` (yellow, red)
- If `cyan()` doesn't exist in `chase.fmt`, add it following the same pattern as the other color helpers

**9. `.claude-task.md` and `.claude-review.md` not in `.gitignore`**
File: `.gitignore`
- Add `.claude-task.md` and `.claude-review.md`

## Constraints
- Pure Python stdlib only — no pip dependencies
- Keep existing function signatures unchanged
- Add logging (use `logging.getLogger(__name__)`) — no print statements
- Do NOT modify README.md or README_CN.md
- Do NOT modify docs/ directory
