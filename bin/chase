#!/bin/bash
# Chase CLI — fallback wrapper when pip install -e . is not available.
# Auto-detect python3 (no hardcoded version).
CHASE_HOME="${CHASE_HOME:-$HOME/Project/chase-harness}"
PYTHON="$(command -v python3 || command -v python)"
PYTHONPATH="$CHASE_HOME" "$PYTHON" -m chase "$@"
